// condiçoes aninhadas (uma função dentro da outra), se a priemeira condição for falsa executará o segundo if

if (1 > 2) {
  true;
} else {
  if (condition) {
    true;
  } else {
    false;
  }
}

// condição multipla

switch (expressão) {
  case valor1: // o switch vai reproduzir todos os cases abaixo da condição se não colocar o break
  
    break
  case valor2:
  
    break
  case valor3:

    break
  default:

    break
}